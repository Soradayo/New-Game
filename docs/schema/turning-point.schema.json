{
  "$id": "https://new-game.local/schema/turning-point.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Turning Point",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "label", "description", "category", "weight", "ageWindow", "conditions", "choices"],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9][a-zA-Z0-9-]*$"
    },
    "label": { "type": "string", "minLength": 1 },
    "description": { "type": "string", "minLength": 1 },
    "category": {
      "enum": ["education", "career", "relationship", "world"]
    },
    "weight": {
      "type": "number",
      "exclusiveMinimum": 0
    },
    "ageWindow": {
      "type": "object",
      "additionalProperties": false,
      "required": ["minMonths", "maxMonths", "guaranteedByMonths"],
      "properties": {
        "minMonths": { "type": "number", "minimum": 0 },
        "maxMonths": { "type": "number", "minimum": 0 },
        "guaranteedByMonths": { "type": "number", "minimum": 0 }
      }
    },
    "conditions": {
      "type": "array",
      "items": { "$ref": "https://new-game.local/schema/condition.schema.json" }
    },
    "choices": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "label", "description", "requirements", "outcomeSummary", "effects", "grantsTags"],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-zA-Z0-9][a-zA-Z0-9-]*$"
          },
          "label": { "type": "string", "minLength": 1 },
          "description": { "type": "string", "minLength": 1 },
          "requirements": {
            "type": "array",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["condition", "reason"],
              "properties": {
                "condition": { "$ref": "https://new-game.local/schema/condition.schema.json" },
                "reason": { "type": "string", "minLength": 1 }
              }
            }
          },
          "outcomeSummary": { "type": "string", "minLength": 1 },
          "effects": {
            "type": "array",
            "items": { "$ref": "https://new-game.local/schema/effect.schema.json" }
          },
          "grantsTags": {
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1
            }
          },
          "educationLevel": {
            "enum": ["none", "primary", "secondary", "vocational", "higher", "nightSchool"]
          },
          "careerCategory": {
            "enum": ["none", "labor", "clerical", "academic", "religious", "state", "underground", "mercantile"]
          },
          "npcOutcomes": {
            "type": "array",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["role"],
              "properties": {
                "role": {
                  "type": "string",
                  "minLength": 1
                },
                "educationLevel": {
                  "enum": ["none", "primary", "secondary", "vocational", "higher", "nightSchool"]
                },
                "careerCategory": {
                  "enum": ["none", "labor", "clerical", "academic", "religious", "state", "underground", "mercantile"]
                },
                "grantsTags": {
                  "type": "array",
                  "items": {
                    "type": "string",
                    "minLength": 1
                  }
                },
                "bond": { "type": "number" },
                "log": { "type": "string", "minLength": 1 }
              }
            }
          }
        }
      }
    }
  }
}
